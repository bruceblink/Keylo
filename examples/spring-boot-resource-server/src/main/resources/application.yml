server:
  address: 127.0.0.1

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYLO_ISSUER}

keylo:
  authorization-base-url: ${KEYLO_AUTHORIZATION_BASE_URL:${KEYLO_ISSUER}}
  required-audience: ${KEYLO_REQUIRED_AUDIENCE:admin-backend}
