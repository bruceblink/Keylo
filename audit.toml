[advisories]
ignore = [
    # Transitive via sqlx-macros-core -> sqlx-mysql; no patched release available upstream yet.
    "RUSTSEC-2023-0071",
    # rand unsound with custom logger; transitive dep, no actionable fix.
    "RUSTSEC-2026-0097",
]
